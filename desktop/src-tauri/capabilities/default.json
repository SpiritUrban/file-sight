{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions: native pickers and revealing a file/folder in Explorer. No shell execution is exposed to the frontend; the Python worker is spawned and constrained by Rust. Thumbnail reads are limited by the assetProtocol scope in tauri.conf.json.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "dialog:allow-open",
    "dialog:allow-message",
    "dialog:allow-confirm",
    "opener:allow-reveal-item-in-dir",
    "opener:allow-open-path"
  ]
}
