# Loaded only by `npm run dev:mock` (vite --mode mock).
# Drives the UI from the in-memory mock worker: no Python, no FFmpeg,
# no model. Never used by `npm run dev` or a production build.
VITE_MOCK_WORKER=1
